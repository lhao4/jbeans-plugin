<idea-plugin>
    <id>com.jbeans.debug.plugin</id>
    <name>JBeans</name>
    <vendor email="" url="https://github.com/lhao4">lhao4</vendor>
    <description><![CDATA[
        <p>Invoke Spring Bean, service implementation, static, and ordinary Java methods directly from your IDE — no test code, no curl, no Postman.</p>

        <p>JBeans uses the JVM Attach API to connect to your running Spring Boot process, injects a lightweight
        in-memory agent, and lets you call any Bean method, static utility method, or HTTP endpoint through a
        clean tool window UI.</p>

        <h3>Features</h3>
        <ul>
            <li><b>Bean Invocation</b> — Browse Spring Beans by name or class, edit JSON parameters, invoke,
                and see the serialized result instantly.</li>
            <li><b>Universal Invocation</b> — Call Spring Bean methods, service implementations,
                ordinary no-arg classes, and public static methods,
                with the same JSON parameter editor.</li>
            <li><b>HTTP Testing</b> — Scan <code>@RequestMapping</code> routes; auto-fill path variables,
                query params, and request body; send requests and view the raw response.</li>
            <li><b>Call History</b> — Every invocation is persisted across IDE restarts; replay or star
                entries for quick access.</li>
            <li><b>Zero Config</b> — No agents to pre-install, no changes to your application code or
                build scripts.</li>
        </ul>

        <h3>Requirements</h3>
        <ul>
            <li>IntelliJ IDEA 2022.3 through 2025.x</li>
            <li>Target JVM: JDK 8 through 21</li>
            <li>Spring Boot applications running locally, including common 2.x / 3.x generation stacks</li>
        </ul>
    ]]></description>

    <!-- 读取 gradle.properties 中的 platformVersion -->
    <idea-version since-build="223"/>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.java</depends>
    <depends>com.intellij.java</depends>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="JBeans"
                    anchor="right"
                    factoryClass="com.jbeans.debug.plugin.ui.DebugToolWindowFactory"
                    icon="/icons/jbeans-tester.svg"/>

        <projectService serviceImplementation="com.jbeans.debug.plugin.service.JbeansProjectService"/>
    </extensions>
</idea-plugin>
